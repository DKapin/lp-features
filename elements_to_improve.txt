### 2.2 `main_content_word_count`
- **Type**: Integer (count)
- **What**: Word count from main content area only
- **Method**:
  1. Target main content: `$('main, .content, article, [role="main"]')`
  2. Same processing as total_word_count
- **Limitations**:
  - May miss content if page doesn't use semantic HTML
  - Multiple main areas are combined
- **Example**: `892`


### 3.4 `has_hero_headline`
- **Type**: Binary (0/1)
- **What**: Whether an H1 exists in the hero section
- **Method**:
  1. Check for H1 in hero area: `$('.hero h1, .hero-section h1, [class*="hero"] h1')`
  2. Convert to binary: `.length > 0 ? 1 : 0`
- **Limitations**:
  - Depends on page using "hero" in class names
  - May miss hero headlines with different naming
- **Example**: `1`

### 4.1 `total_cta_count`
- **Type**: Integer (count)
- **What**: Total number of CTA elements on page
- **Method**: Count elements matching:
  - `button`
  - `a.btn`
  - `a.button`
  - `[role="button"]`
  - `.cta`
  - `[class*="cta-"]`
- **Limitations**:
  - May count non-CTA buttons (close, menu toggle)
  - May miss CTAs with unusual markup
- **Example**: `15`

### 4.2 `primary_cta_count`
- **Type**: Integer (count)
- **What**: CTAs in hero/header area
- **Method**: Count elements matching:
  - `$('.hero button, .hero a.btn, header button, [class*="hero"] button')`
- **Limitations**:
  - Depends on semantic class names
  - May miss CTAs in unnamed hero sections
- **Example**: `2`

### 4.3 `form_count`
- **Type**: Integer (count)
- **What**: Number of `<form>` elements
- **Method**: `$('form').length`
- **Limitations**:
  - Doesn't check if forms are visible or functional
  - May count hidden forms
- **Example**: `3`

### 4.4 `form_field_count`
- **Type**: Integer (count)
- **What**: Total visible form fields
- **Method**: Count:
  - `$('form input[type!="hidden"]')` (all inputs except hidden)
  - `$('form textarea')` (text areas)
  - `$('form select')` (dropdowns)
- **Limitations**:
  - Doesn't verify if fields are actually visible
  - May include disabled fields
- **Example**: `5`

### 4.5 `has_email_capture`
- **Type**: Binary (0/1)
- **What**: Whether page has email input field
- **Method**: Check for:
  - `$('input[type="email"]')` OR
  - `$('input[name*="email"]')`
- **Limitations**:
  - May miss email fields with unconventional naming
- **Example**: `1`


### 5.2 `hero_image_count`
- **Type**: Integer (count)
- **What**: Images in hero section
- **Method**: `$('.hero img, .hero-section img, [class*="hero"] img').length`
- **Limitations**: Depends on hero class naming
- **Example**: `1`

### 5.3 `has_hero_image`
- **Type**: Binary (0/1)
- **What**: Whether hero section has an image
- **Method**: `hero_image_count > 0 ? 1 : 0`
- **Limitations**: Same as hero_image_count
- **Example**: `1`



### 6.2 `has_testimonials`
- **Type**: Binary (0/1)
- **What**: Whether page has any testimonials
- **Method**: Check for testimonial/review classes: `.length > 0 ? 1 : 0`
- **Limitations**: Same as testimonial_count
- **Example**: `1`

### 6.3 `rating_element_count`
- **Type**: Integer (count)
- **What**: Star ratings or rating displays
- **Method**: Count elements with classes:
  - `.rating`
  - `[class*="star"]`
  - `[class*="rating"]`
- **Limitations**:
  - May count rating systems that aren't reviews
  - Doesn't verify actual rating values
- **Example**: `3`

### 6.4 `trust_badge_count`
- **Type**: Integer (count)
- **What**: Security/trust badges
- **Method**: Count images with alt text containing:
  - "secure"
  - "certified"
  - "badge"
- **Limitations**:
  - Only detects badges via alt text
  - May miss CSS-based badges
- **Example**: `2`


### 6.5 `logo_count`
- **Type**: Integer (count)
- **What**: Client/partner logos displayed
- **Method**: Count images in:
  - `.logos img`
  - `.clients img`
  - `.partners img`
  - `[class*="logo-grid"] img`
- **Limitations**: Depends on semantic class naming
- **Example**: `12`


### 6.6 `has_social_proof`
- **Type**: Binary (0/1)
- **What**: Any social proof elements present
- **Method**: Check for classes containing:
  - "social-proof"
  - "customer"
  - "users"
- **Limitations**: Broad detection, may have false positives
- **Example**: `1`


### 7.1 `section_count`
- **Type**: Integer (count)
- **What**: Number of `<section>` tags
- **Method**: `$('section').length`
- **Limitations**:
  - Only counts semantic `<section>` tags
  - Doesn't count div-based sections
- **Example**: `8`


### 8.2 `benefit_section_count`
- **Type**: Integer (count)
- **What**: Sections describing benefits
- **Method**: Count elements with:
  - `.benefits`
  - `[class*="benefit"]`
  - `[id*="benefit"]`
- **Limitations**: Depends on semantic naming
- **Example**: `2`

### 8.3 `pricing_section_present`
- **Type**: Binary (0/1)
- **What**: Whether page has pricing section
- **Method**: Check for:
  - `.pricing`
  - `[class*="pricing"]`
  - `[id*="pricing"]`
- **Limitations**: May miss pricing in other sections
- **Example**: `1`

### 8.4 `faq_section_present`
- **Type**: Binary (0/1)
- **What**: Whether page has FAQ section
- **Method**: Check for:
  - `.faq`
  - `[class*="faq"]`
  - `[id*="faq"]`
- **Limitations**: Depends on semantic naming
- **Example**: `1`


### 13.1 `has_comparison_table`
- **Type**: Binary (0/1)
- **What**: Comparison table or vs section
- **Method**: Check for:
  - `$('table')`
  - `[class*="comparison"]`
  - `[class*="vs"]`
- **Limitations**: May detect non-comparison tables
- **Example**: `1`

### 16.4 `hero_cta_text_length`
- **Type**: Integer (count)
- **What**: Character length of hero CTA text
- **Method**:
  1. Find hero CTA: `$('.hero button, .hero a.btn, [class*="hero"] button').first()`
  2. Get text: `.text().trim()`
  3. Count: `.length`
- **Limitations**:
  - Depends on hero class naming
  - Returns 0 if no hero CTA found
- **Example**: `14` (for "Get started now")

### 20.1 `testimonial_has_photo`
- **Type**: Binary (0/1)
- **What**: Testimonials include photos
- **Method**: Check for images within testimonial elements:
  - `$('[class*="testimonial"] img')`
  - `$('[class*="review"] img')`
  - `$('blockquote img')`
- **Rationale**: Photos increase authenticity by 40%
- **Limitations**:
  - May detect decorative images
  - Doesn't verify photos are of people
- **Example**: `1`


### 20.5 `case_study_present`
- **Type**: Binary (0/1)
- **What**: Case study or success story section exists
- **Method**: Check for classes/IDs:
  - `[class*="case-study"]`
  - `[class*="case study"]`
  - `[class*="success-story"]`
  - `[class*="customer-story"]`
- **Limitations**: Depends on semantic naming
- **Example**: `0`

### 21.2 `free_trial_mentioned`
- **Type**: Binary (0/1)
- **What**: Free trial offer mentioned
- **Method**:
  1. Search body text: `/free trial|try free|no credit card|start free/i`
  2. Convert to binary
- **Rationale**: Free trials increase sign-ups by 30-50%
- **Limitations**: Doesn't extract trial duration
- **Example**: `1`


### 27.1 `exit_popup_present`
- **Type**: Binary (0/1)
- **What**: Exit intent popup exists
- **Method**: Check for:
  - `[class*="exit"]`
  - `[id*="exit"]`
- **Rationale**: Recovers 10-15% of abandoning visitors
- **Limitations**:
  - Detects in DOM, not if triggered
  - Pattern-based
- **Example**: `0`


## 27.3 `sticky_cta`
- **Type**: Binary (0/1)
- **What**: CTA remains visible when scrolling
- **Method**: Check for:
  - `[class*="sticky"][class*="cta"]`
  - `[class*="fixed"][class*="cta"]`
  - `[style*="position: fixed"]`
- **Rationale**: Maintains conversion opportunity
- **Limitations**:
  - Pattern and inline style based
  - May miss JS-controlled sticky CTAs
- **Example**: `0`